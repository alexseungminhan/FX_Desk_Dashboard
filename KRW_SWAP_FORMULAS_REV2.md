# 원화 스왑 계산식 — 개정 2판 (par rate 기준)

`KRW_SWAP_FORMULAS.md` 를 데스크 pricer(2026-08-10 고시)와 대조해 검증한 결과와
그에 따른 수정 지침. 기존 문서의 STEP 1·2는 **검증 통과**했고, STEP 3·4와
입력값 취득 방식에 수정이 들어간다.

---

## 0. 개정 요약

| 항목 | 기존 | 개정 | 1Y basis 영향 |
|---|---|---|---|
| 스팟일 | 실행 시각 기준 T+2 | **고시일 기준 T+2** | 만기별 상이 (§1) |
| 스팟 레벨 | 야후 실시간 | **고시 스냅샷과 동기화** | 0.2bp |
| STEP 2 implied yield | 단리 ACT/365 zero | 유지 (중간 산출물로 강등) | — |
| **STEP 3 (신규)** | — | **DF 구성 → 분기 par swap rate** | **3.9bp** |
| STEP 4 basis | zero − IRS | **par − IRS** | (위에 포함) |
| 6M IRS 보간 | (a) 일수선형 채택 | (a)/(b) 가중 재검토 | 4.3bp (6M) |
| `basis360Bp` 검증열 | ACT/360 basis | **`yieldSimple`(단리 zero)로 교체** | — |

**가장 큰 오차원은 par 미적용이다.** 3M 이하는 영향 0, 6M −1.2bp, 9M −2.5bp,
1Y −3.9bp, 2Y −10bp 로 만기에 비례해 커진다.

### 검증의 근거

pricer 시트의 `KRW Zero` 열이 기존 STEP 2 결과와 소수 4자리까지 일치한다.
곱셈식·ACT/360 달러다리·ACT/365 연율화는 전부 맞다는 뜻이다.
`KRO FX%` 열(= 화면에 쓰이는 값)만 다른 물건이었고, 그것이 par swap rate다.

| 만기 | 단리 zero | 분기복리 zero | **par swap** | pricer `KRO FX%` |
|---|---|---|---|---|
| 3M | 3.1272 | 3.1271 | 3.1272 | 3.1266 (−0.06bp) |
| 6M | 3.0572 | 3.0454 | 3.0458 | 3.0451 (−0.07bp) |
| 1Y | 3.1780 | 3.1408 | 3.1401 | 3.1393 (−0.08bp) |
| 18M | 3.3679 | 3.2986 | 3.2956 | 3.2950 (−0.06bp) |
| 2Y | 3.4331 | 3.3337 | 3.3306 | 3.3300 (−0.06bp) |

par 오차는 만기와 무관하게 **평평한 0.06~0.08bp** (pricer가 DF를 4자리로
반올림해 내보내는 데서 오는 상수 오프셋). 분기복리 zero 오차는 만기에 비례해
0.03 → 0.37bp 로 벌어지므로 기각한다.

---

## 1. 스팟일 — 고시일 기준으로 고정

**증상**: 1M 32일(pricer 33일), 6M 187일(pricer 184일).

**원인**: `fx_calendar` 가 실행 시각(`today`)에서 T+2 를 잡는데, 스왑포인트는
전영업일 고시분이다. 고시일 2026-08-10(월) → 스팟 08-12(수)가 맞는데
실행일 08-11(화) 기준으로 08-13(목)이 잡혔다.

6M 은 여기서 한 번 더 증폭됐다. 스팟 08-13 → 6M 밸류 2027-02-13(토)
→ 02-15(월)인데 **미국 Presidents' Day**라 02-16(화)로 밀려 187일.
스팟이 08-12였다면 2027-02-12(금)로 깔끔하게 184일이다.
캘린더 로직은 정상이고 시작점만 틀렸다.

```
수정: fx_calendar.spot_date(quote_date)   # today 가 아니라 고시일을 인자로
     krw_swap 이 스크래핑 시 고시일자를 같이 파싱해 넘긴다
     (SMBS·KMB 페이지 헤더의 "26.08.10 고시")
```

**일수 1일당 basis 민감도** — 스왑포인트 연율을 일수로 나눈 값과 같다:

| 만기 | 1일당 |
|---|---|
| 1M | 1.5bp |
| 3M | 0.7bp |
| 6M | 0.46bp |
| 1Y | 0.24bp |

짧은 만기일수록 치명적이다. 1M 에서 하루 어긋나면 basis가 1.5bp 움직인다.

---

## 2. 스팟 레벨 — 고시와 같은 스냅샷

pricer의 `KRW Zero` 에서 스팟을 역산하면 만기별로

```
1M 1418.97   3M 1418.98   6M 1418.86   1Y 1418.98
```

네 만기가 전부 1418.9 로 모인다. 야후에서 받은 1415.29 와 3.6원 차이.
스왑포인트는 08-10 고시인데 스팟만 실시간이라 시점이 어긋난 것이다.

**수정**: 고시일 서울 종가(또는 매매기준율)를 쓰고, 실시간 스팟은 별도 열로만
표시한다. `spotSource` / `spotAsOf` 를 값과 함께 들고 다닌다.

민감도는 크지 않다(1Y yield 기준 3.6원 = 0.23bp)지만, 스왑포인트 연율 표시가
스팟에 직접 비례하므로 화면 일관성 차원에서 맞추는 게 맞다.

---

## 3. USD 텀금리 — 현행 유지

Term SOFR 대조 결과 문제없음.

| 만기 | 우리 값 | pricer | 차 |
|---|---|---|---|
| 1M | 3.6404% | 3.6399% | 0.05bp |
| 3M | 3.7382% | 3.7386% | 0.04bp |
| 6M | 3.8562% | 3.8590% | 0.28bp |
| 1Y | 4.0062% | 4.0170% | **1.09bp** |

1Y 만 1.1bp 벌어진다. yield 에 거의 1:1 로 전가되므로($\partial y/\partial u \approx 1.01$)
basis 1.1bp 짜리 오차다. 스크래핑 시각 차이로 보이며, 기존 문서의
"소스를 바꾸면 10~20bp" 경고 대비 훨씬 양호하다. **`UST_CMT` 로 강등되지 않는 한
현행 유지.**

---

## 4. STEP 1·2 — 변경 없음 (검증 통과)

### STEP 1 — swap rate

$$sr = \frac{P}{S}\times\frac{360}{d}$$

기존대로 ACT/360. 단, **pricer 의 `Swap rate` 열은 ACT/365 다**
(1Y −0.86% = −12.20/1418.9 그대로). 둘 다 표시 관습이고 틀린 건 없지만,
pricer 와 나란히 볼 일이 많으므로 각주에 명시하거나 ACT/365 열을 병기한다.
차이는 전 만기 일률 365/360 = 1.39%.

### STEP 2 — growth factor & 단리 zero

$$G = \left(1+\frac{P}{S}\right)\left(1+u\cdot\frac{d}{360}\right)-1$$

$$y_{\text{simple}} = G\times\frac{365}{d}$$

근사식 $u+sr$ 을 쓰지 않는 것, 달러다리 360 을 그대로 두는 것, 마지막 연율화만
365 로 가는 것 — **전부 pricer 와 동일**하다. `crossTermBp` 열도 유지.

다만 **$y_{\text{simple}}$ 은 이제 최종값이 아니라 중간 산출물**이다.
화면에는 par rate 를 쓰고, 단리값은 검증열(`yieldSimple`)로만 남긴다.
pricer 의 `KRW Zero` 열과 직접 대조 가능한 유일한 지점이라 회귀 테스트에 쓴다.

---

## 5. STEP 2.5 (신규) — 할인계수와 분기 그리드

### 5.1 DF 구성

$$DF_i = \frac{1}{1+G_i}$$

$G$ 는 컨벤션이 없는 순수 증가율이므로 여기서 일수/365 를 나누면 안 된다.
**연율화 이전의 $G$ 에서 바로 DF 를 만든다.**

### 5.2 분기 지급일 스케줄

par rate 의 annuity 에 들어갈 날짜는 KRW IRS 지급 관습을 따른다 —
**스팟에서 3개월 간격, modified following, 월말규칙 동일**.
`fx_calendar.tenor_schedule()` 을 그대로 재사용하면 3M/6M/9M/1Y 밸류데이트와
일치한다.

```
스팟 2026-08-12 기준
  3M  2026-11-12   92일
  6M  2027-02-12  184일
  9M  2027-05-12  273일
  1Y  2027-08-12  365일
```

### 5.3 9M DF 보간

9M 스왑포인트는 양사 모두 고시가 없다. **log-linear DF 보간**을 쓴다
(구간 내 연속복리 선도금리 일정 = 음의 선도금리가 나올 수 없음):

$$\ln DF(t) = \ln DF_{lo} + (\ln DF_{hi}-\ln DF_{lo})\frac{t-t_{lo}}{t_{hi}-t_{lo}}$$

**6M IRS 보간과 달리 여기서는 방식 논쟁이 필요 없다.** 9M 은 annuity 가중치로만
들어가 오차가 크게 희석되기 때문:

| 9M zero 오차 | 1Y par 변화 |
|---|---|
| ±10bp | ±0.06bp |
| ±20bp | ±0.11bp |

실측 대조 — pricer 의 실제 9M 포인트(−9.10)로 만든 DF 0.977211 vs
6M·1Y 만으로 log-linear 보간한 DF 0.977109 (zero 기준 1.4bp 차이).
**1Y par 은 3.1401% → 3.1402%, 0.01bp 움직인다.**
경고 배지도 필요 없다.

---

## 6. STEP 3 (신규) — par swap rate

$$\boxed{\;\text{par} = \frac{1-DF_N}{\sum_{i=1}^{N} DF_i\,\tau_i}\;},\qquad
\tau_i = \frac{t_i - t_{i-1}}{365},\quad t_0 = \text{스팟}$$

- 고정·변동 양다리 모두 분기, ACT/365 (KRW IRS 관습)
- 변동다리 PV 를 $1-DF_N$ 으로 두는 단일커브 가정 (§10 한계 참조)
- 만기 자체가 $t_N$, 중간 지급일이 $t_1 \dots t_{N-1}$

### 만기 ≤ 3M 은 자동으로 단리와 같아진다

지급이 한 번뿐이면 $DF_N = 1/(1+G)$ 를 대입해

$$\text{par} = \frac{1-DF_N}{DF_N \tau} = \frac{G}{\tau} = G\times\frac{365}{d} = y_{\text{simple}}$$

**특수 분기를 넣을 필요가 없다.** 1M·3M 은 par 식을 그대로 통과시키면 기존 값이
그대로 나온다 — 실제로 우리 3M 이 pricer 와 0.14bp 로 붙어 있던 이유가 이것이다.

### 왜 par 여야 하나

빼줄 상대인 **KRW IRS 고시 자체가 par swap rate** 다. par − par 이어야 같은
물건끼리 빼는 것이고, zero − par 는 만기가 길수록 구조적으로 벌어진다.

기존 문서의 "ACT/365 로 펴 두면 분모가 같아져 그냥 빼면 된다"는
**일수 관습만 맞춘 서술**이었다. 복리·지급주기 관습은 아직 맞지 않은 상태였다.

### 중요 — FX 스왑에 중간 현금흐름은 없다

par 로 바꾸는 것은 **표기 변환**이지, FX 스왑에 없던 이자지급을 만드는 게 아니다.
FX 스왑은 스팟 한 번, 만기 한 번이 전부다. 우리가 하는 일은 FX-implied
**할인커브**를 IRS 고시 관습으로 다시 읽는 것이다.

여기서 나오는 실무적 귀결이 중요하다:

> **1Y par 은 1Y 스왑포인트 하나로는 만들 수 없다.**
> annuity 에 3M·6M·9M DF 가 전부 들어가므로 커브 전체를 먼저 세워야 한다.
> `fx_implied.compute` 는 만기별 독립 루프에서 **커브 단위 계산**으로 구조가 바뀐다.

---

## 7. STEP 4 — CCS basis

$$\text{basis(bp)} = (\text{par} - \text{IRS})\times 10000$$

`basis360Bp` 검증열은 폐기한다. 양변 관습이 이제 일수·복리 모두 맞았으므로
ACT/360 위의 basis 는 의미가 없다. 대신 **`yieldSimple` 열을 남겨**
pricer `KRW Zero` 와 대조하는 용도로 쓴다.

### 만기 미스매치 주의 (기존 이슈, 미해결)

1M·3M basis 는 IRS 자리에 **CD 91일물**을 쓰고 있다. 1M par(32~33일)을 91일물과
빼는 것이므로 커브 기울기만큼 왜곡이 있다. pricer 도 1M/3M `KRO IRS` 가
2.9299 / 2.9301 로 사실상 같은 값이라 같은 한계를 공유한다.
**1M basis 는 "3M CD 대비"라고 각주를 붙여 읽는다.**

---

## 8. 6M IRS 보간 — 채택 방식 재검토

pricer 의 실측 6M IRS = **3.1527%**.

| 방식 | 값 | 실측과의 차 |
|---|---|---|
| (a) 일수 선형 | 3.1101% | −4.26bp |
| (b) log-DF 선형 | 3.2656% | +11.29bp |
| 실측 | 3.1527% | — |

기존 문서의 예측 — "실측 호가는 보통 (a)와 (b) 사이에 있다" — 이 맞았고,
폭 15.6bp 경고도 제 역할을 했다. 다만 **(a)를 그대로 채택한 것이 6M basis 에서
4.3bp 를 벌었다.**

실측 위치는 (a)에서 (b) 쪽으로 27% 지점이다. 데이터포인트 1개로 가중치를
고정하는 건 위험하므로 다음 순서로 처리한다:

1. 당분간 **(a) 유지**, 단 화면에 (a)/(b)/실측 대조 로그를 쌓는다
2. 관측 5~10개 누적 후 위치 분포를 보고 가중 $w$ 결정
3. 커브 기울기(CD−IRS 1Y 간격)를 설명변수로 넣을 수 있는지 확인
   — 가팔라질수록 (b) 쪽으로 붙을 개연성이 있다

밴드 경고 로직(`max(5bp, spreadBp)`, `|basis| ≤ 밴드` 시 방향 판독 금지)은
**par 기준 basis 위에서 그대로 유지**한다.

---

## 9. 검증 결과 — 회귀 테스트 기준값

고시일 2026-08-10 / 스팟일 2026-08-12 / 스팟 1418.90 / USD Term SOFR

| 만기 | 일수 | 포인트(원) | USD | $G$ | DF | `yieldSimple` | **par** | KRO IRS | **basis** |
|---|---|---|---|---|---|---|---|---|---|
| 1M | 33 | −0.65 | 3.6399% | 0.00287695 | 0.997131 | 3.1821% | 3.1821% | 2.9299% | +25.22bp |
| 3M | 92 | −2.35 | 3.7386% | 0.00788216 | 0.992179 | 3.1272% | 3.1272% | 2.9301% | +19.71bp |
| 6M | 184 | −6.00 | 3.8590% | 0.01541175 | 0.984822 | 3.0572% | 3.0458% | 3.1527% | −10.69bp |
| 1Y | 365 | −12.20 | 4.0170% | 0.03177952 | 0.969199 | 3.1780% | 3.1401% | 3.4473% | −30.72bp |

pricer 대조:

| 만기 | 우리 par | pricer `KRO FX%` | 차 | 우리 basis | pricer basis | 차 |
|---|---|---|---|---|---|---|
| 1M | 3.1821 | 3.1819 | 0.02bp | +25.22 | +25.20 | 0.02bp |
| 3M | 3.1272 | 3.1266 | 0.06bp | +19.71 | +19.65 | 0.06bp |
| 6M | 3.0458 | 3.0451 | 0.07bp | −10.69 | −10.76 | 0.07bp |
| 1Y | 3.1401 | 3.1393 | 0.08bp | −30.72 | −30.81 | 0.08bp |

잔차가 만기와 무관하게 0.02~0.08bp 로 평평하다 = 모델이 맞다는 신호.
(pricer 가 DF 를 4자리로 반올림해 내보내는 데서 오는 상수 오프셋)

### 개정 전후 비교

| 만기 | 개정 전 basis | 개정 후 | pricer |
|---|---|---|---|
| 1M | +23.54 | +25.22 | +25.20 |
| 3M | +19.51 | +19.71 | +19.65 |
| 6M | −4.44 | −10.69 | −10.76 |
| 1Y | −28.27 | −30.72 | −30.81 |

---

## 10. 오차 예산 (1Y basis 기준)

| 원인 | 크기 | 상태 |
|---|---|---|
| par 미적용 | 3.9bp | §6 로 해소 |
| USD 텀금리 소스 | 1.1bp | 잔존, 스크래핑 시각 동기화 필요 |
| 스팟 레벨 3.6원 | 0.23bp | §2 로 해소 |
| 스팟일 1일 | 0.24bp (1Y) / 1.5bp (1M) | §1 로 해소 |
| 9M DF 보간 | 0.01bp | 무시 가능 |
| pricer DF 반올림 | 0.08bp | 대조 불가, 하한 |

**6M 은 별도** — IRS 보간이 4.3bp 로 단일 최대 오차원이며 §8 이후에도 잔존한다.
6M basis 는 계속 구간으로 읽는다.

---

## 11. 구현 체크리스트

```
fx_calendar.py
  [ ] spot_date(quote_date) — today 대신 고시일 인자
  [ ] quarterly_schedule(spot, n_quarters) — 3M 간격 mod-following, EOM 규칙 공유

krw_swap.py
  [ ] 스크래핑 시 고시일자 파싱 → fx_implied 로 전달
  [ ] 스팟을 고시일 종가로 교체, spotSource/spotAsOf 동반
  [ ] 연율 표 각주에 "ACT/360 (pricer 는 ACT/365)" 명시

fx_implied.py
  [ ] 만기별 독립 루프 → 커브 단위 2-pass 로 구조 변경
      pass 1: 만기별 G, DF 계산
      pass 2: 분기 그리드 DF 보간 → 만기별 par rate
  [ ] df_from_growth(G) = 1/(1+G)
  [ ] interp_df_loglinear(t, pillars)
  [ ] par_rate(schedule, DF) = (1-DF_N)/Σ DF_i·τ_i
  [ ] 출력에 yieldSimple, parRate 둘 다 포함
  [ ] basis = (parRate − IRS)×10000
  [ ] basis360Bp 제거
  [ ] 6M 밴드 경고 로직을 par basis 위로 이설
  [ ] 9M 이 보간값임을 pillarSource 에 기록 (경고 배지는 불필요)

테스트
  [ ] §9 기준값으로 회귀 테스트 고정
  [ ] 만기 ≤3M 에서 par == yieldSimple 인지 assert (식의 자기일관성)
  [ ] 6M IRS (a)/(b)/실측 로그 적재 시작
```

---

## 12. 남은 한계

**단일커브 가정** — 변동다리 PV 를 $1-DF_N$ 으로 둔다. 실제 데스크는 CCS 로
할인하는 멀티커브를 쓰므로 완전히 같지 않다. pricer 와 0.08bp 로 붙는 걸 보면
현재 시장 상황에서는 무시 가능한 수준이지만, basis 가 크게 벌어지는 국면에서는
재검토가 필요하다.

**9M 미고시** — 보간 의존. 현재 민감도 0.01bp 라 문제없으나, 커브가 9M 근처에서
꺾이는 국면(정책금리 변곡 기대가 특정 시점에 몰릴 때)에는 확인이 필요하다.

**1M basis 의 만기 미스매치** — §7 참조. CD 91일물을 상대로 쓰는 한 해소되지 않는다.

**USD 텀금리** — Term SOFR 스크래핑 의존. `UST_CMT` 로 강등되면 bill-OIS
스프레드만큼(10~20bp) basis 가 양(+)쪽으로 치우친다는 기존 경고는 그대로 유효하다.

---

## 관련 파일

| 파일 | 역할 | 개정 |
|---|---|---|
| `krw_swap.py` | 패널 조립, 연율 환산, CRS−IRS | 고시일·스팟 소스 |
| `fx_implied.py` | STEP 1~4 계산기 | **커브 단위 2-pass 로 재구성** |
| `fx_calendar.py` | 스팟·밸류데이트·실제일수 | 고시일 인자, 분기 스케줄 |
| `usd_rates.py` | USD 텀금리 4종 + ACT/360 환산 | 변경 없음 |
| `smbs.py` / `kmb.py` | 중개사 고시 스크래핑 | 고시일자 파싱 추가 |
